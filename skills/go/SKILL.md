---
name: go
description: /go 全自动编排 —— 输入功能描述,全自动任务分级→深度需求分析→最佳方案推荐→拆分→多工具调度→断点恢复→降级兜底→全局回归→交付。全程无人干预。
metadata:
  version: "2.0"
  type: slash-command
  mode: multi-tool-orchestration-auto
---

# /go — 全自动编排

你是 `/go` 命令的自动执行引擎。

## ⚠️ 硬规则(不可违反)

> **必须先深入分析项目实际环境,才能推荐方案并执行。这是强制性前置条件。**

违反意味着:
- 禁止: 不读项目文档就推荐方案
- 禁止: 不扫描现有模块就决定"新增一个模块"
- 禁止: 不了解技术栈就选型
- 禁止: 不检查现有代码就写新代码(导致重复造轮子)

**你可以不等人确认,但你不能不分析项目就做决定。**

## 命令格式

```
/go 功能描述，验收条件1，验收条件2，...
/go-fast 功能描述                # 强制 L1 直通档
/go-full 功能描述                # 强制 L3 完整档
```

## 核心原则:全程自动化

> **所有需要确认的地方,系统自动推荐方案、自动执行、自动记录推荐理由。**
> 不使用硬闸门暂停等用户——全部改为**审计闸门**(自动通过+记录理由,可追溯但不等停)。

**自动决策三原则**:
1. **推荐**: 分析项目上下文→给出最优方案
2. **执行**: 验证通过后直接执行,不等人确认
3. **记录**: 决策理由写入 `decision_log`,交付报告追溯

**例外(物理限制,无法自动化)**:
- Cursor 前端任务(v1无CLI,需用户手动切到Cursor写代码)——这是工具限制,不是逻辑闸门
- Trae 审查任务(v1无CLI)——同上

---

## 与 loop 技能的关系

| 层级 | 职责 | 如何协作 |
|------|------|---------|
| go(编排层) | 需求分析→任务拆分→多工具调度→全局回归→交付 | 以最小信号传递子任务上下文 |
| loop(执行层) | 单任务闭环编码:门禁矩阵+自愈+经验注入/沉淀 | ZCode加载loop --auto技能执行子任务 |

go 负责"拆任务、分对工具";loop 负责"把每个任务做对"。go 不碰执行层细节。

---

## 执行流程

```
/go 触发
    │
    ▼
┌─────────────────────────────────────────────────┐
│ Step ⓪  断点续跑检测(启动时最先执行)              │
├─────────────────────────────────────────────────┤
│ • 检查 .orchestrate-state.json                   │
│ • 存在且未完成 → 自动续跑(不等用户确认)           │
│   (详见 references/breakpoint-recovery.md)        │
│ • 不存在 → 进入 Step ①                           │
└─────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────┐
│ Step ①  L0 复杂度评估(纯规则 · 零 token)         │
├─────────────────────────────────────────────────┤
│ • 读取 references/complexity-rules.md 规则       │
│ • 检查命令标志(-fast→L1, -full→L3)              │
│ • 否则按信号评估(关键词/文件数/跨工具)            │
│ • 输出: 评估为 [级别], 理由: [...]               │
│ • 📝 记录到 decision_log                        │
└─────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────┐
│ Step ①.5  强制深度需求分析(硬规则 · v2.0核心)     │
├─────────────────────────────────────────────────┤
│ ⚠️ 必须完成以下全部6个维度分析,否则禁止进入Step②  │
│                                                 │
│ 【维度1: 项目身份识别】                            │
│  • 扫描项目文档(自动发现,不硬编码文件名):           │
│    - CLAUDE.md / README.md / CONTRIBUTING.md       │
│    - docs/ 下所有 .md 文件(设计文档/调研报告/方案)  │
│    - deploy/ 下 docker-compose.yml(部署架构)       │
│  • 从中提取: 项目是什么、做什么、怎么部署           │
│  • 产出: 项目类型(全栈Web/纯后端/工具) + 部署方式  │
│                                                 │
│ 【维度2: 技术栈扫描】                              │
│  • 读 pyproject.toml/package.json → 核心技术栈    │
│  • 识别: 前端框架/后端框架/数据库/缓存/存储       │
│  • 产出: 完整技术栈清单 + 版本约束                │
│                                                 │
│ 【维度3: 现有模块盘点】                            │
│  • 扫描 backend/app/* 和 frontend/src/* 顶层目录  │
│  • 识别: 已有哪些业务模块(skill_hub/mcp_hub/...)? │
│  • 产出: 模块清单 + 模块职责描述                  │
│                                                 │
│ 【维度4: 功能匹配分析(★最关键)】                   │
│  • 新功能与哪个现有模块最相关?                     │
│  • 是否可以扩展现有模块(优先)?                     │
│  • 还是需要新建模块?                              │
│  • 哪些已有接口/表可以直接复用?                    │
│  • 产出: 匹配结论 + 复用清单 + 新建清单            │
│                                                 │
│ 【维度5: 架构约束检查】                            │
│  • 所有DB操作是否 async? (项目是async SQLAlchemy)  │
│  • 是否遵循现有的 router→service→model 分层?      │
│  • 是否需要新增迁移(用alembic)?                    │
│  • 前端是否遵循 React+TS+AntD 模式?               │
│  • 产出: 架构约束清单 + 违背风险提醒               │
│                                                 │
│ 【维度6: 风险排查 + 方案生成】                      │
│  • 方案A(推荐): 基于维度4+5分析的最优路径          │
│  • 方案B: 备选路径(何时选?权衡了什么?)             │
│  • 风险评估: 改了核心链路?有数据迁移?              │
│  • 产出: 推荐方案 + 替代方案 + 风险清单            │
│                                                 │
│ ▸ 完成后自动生成《需求分析报告》写入 decision_log   │
│ ▸ 报告结构:                                      │
│   项目: [项目名] · 类型: [全栈Web/后端/工具]      │
│   技术栈: [框架/DB/缓存/存储]                      │
│   现有模块: [列表]                                 │
│   匹配分析: 扩展 [模块名] · 复用 [接口/表]         │
│   推荐方案: [方案A]                                │
│     采纳理由: [基于维度4+5的推理]                  │
│   替代方案: [方案B] · 未采纳理由: [...]            │
│   风险: [高/中/低] · [具体风险]                    │
│                                                 │
│ ▸ 全部6个维度产出完整 → ✅ 准入Step②               │
│ ▸ 任一维度缺失 → ❌ 禁止进入Step②                  │
└─────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────┐
│ Step ②  状态初始化 + 方案提交(📝审计闸门)        │
├─────────────────────────────────────────────────┤
│ • 创建 .orchestrate-state.json                  │
│   (格式见 references/state-protocol.md)          │
│ • 记录 Step①.5 产出的《需求分析报告》全部内容     │
│ • 验收条件 = 用户指定 + 自动推理补全             │
│ • 📝 审计闸门: 自动通过,决策全部记录             │
│   准入条件: Step①.5的6个维度全部产出完整 ✅      │
│   准入失败 → 回到Step①.5补全缺失维度            │
│ • 无需等待用户确认,直接进入 Step③               │
└─────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────┐
│ Step ③  任务拆分(按 tier 裁剪)                  │
├─────────────────────────────────────────────────┤
│ • 🟢 L1: 不拆分,单任务直接执行                  │
│ • 🟡 L2: 按模块拆 2-5 个子任务,串行依赖         │
│ • 🔴 L3: 深度垂直拆分 + 依赖 DAG                │
│   无依赖任务标记可并发(拓扑序调度)              │
│ • 拆分方案自动执行,写入状态文件                  │
└─────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────┐
│ Step ⑤  调度执行(按拓扑序 · 含降级兜底)          │
├─────────────────────────────────────────────────┤
│ 读取 routing-rules.yaml 匹配工具:                │
│                                                 │
│ ▸ ZCode + loop --auto 任务(主力 · 80%+场景):     │
│   1. 生成 prompt:                                │
│      - 注入 "加载 loop 技能（--auto 模式）"       │
│      - 注入项目上下文(最小信号):                  │
│        技术栈/架构约束/前置任务产物指针(不传内容)   │
│      - 注入验收条件                               │
│   2. 记录 git_head_before(原子性回滚用)          │
│   3. 自动设定 GLM-5.2 推理模式                  │
│      L1→off / L2→high / L3→max                │
│   4. 调用: node zcode.cjs --prompt "..."        │
│   5. loop --auto 内部完成:                     │
│      编码→门禁矩阵→自愈闭环→交付                │
│   6. loop exhausted? → 降级 ZCode CLI(无门禁)   │
│   7. 也失败? → DeepSeek 降级兜底               │
│                                                 │
│ ▸ Cursor 任务(半自动):                          │
│   1. 暂停 + 自动记录 git HEAD                   │
│   2. 提示: "请在 Cursor 完成并 commit"           │
│   3. 自动轮询检测 git 变化                       │
│   4. 检测到 commit → 自动续跑                    │
│   (工具限制,Cursor v1无CLI)                     │
│   (详见 references/cursor-collaboration.md)      │
│                                                 │
│ ▸ Trae 任务(半自动):                            │
│   1. 提示用户切到 Trae                            │
│   2. 等待用户完成(同 Cursor——工具限制)           │
│                                                 │
│ ▸ 无依赖任务自动并发(L3 多智能体并行)            │
│ ▸ 每步自动写盘(检查点机制,防中断)               │
│                                                 │
│ ▸ 降级链路(loop失败时):                          │
│   loop exhausted → ZCode CLI → DeepSeek API     │
│   每级降级标记 degraded=true + 原因             │
│   降级执行无门禁保护,质量分层标记                │
└─────────────────────────────────────────────────┘
    │ 所有子任务完成
    ▼
┌─────────────────────────────────────────────────┐
│ Step ⑥  上下文交接(机制⑥ · 已在⑤逐步完成)        │
├─────────────────────────────────────────────────┤
│ 确认所有交接链完整,生成完整交接链报告            │
│ handoff 包含: gate_result(loop门禁摘要)          │
│ (详见 references/handoff-protocol.md)            │
└─────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────┐
│ Step ⑦  全局集成回归(机制⑦)                     │
├─────────────────────────────────────────────────┤
│ • 全量测试套件(覆盖所有子任务合集)               │
│ • 跨模块接口契约自动校验                          │
│ • 前后端联调(loop 门禁覆盖了单任务,go做跨任务)   │
│                                                 │
│ 通过 → 自动进入交付                              │
│ 失败 → 自动回退失败子任务重做(不等人)            │
└─────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────┐
│ Step ⑧  交付(📝审计闸门 · 自动合并)               │
├─────────────────────────────────────────────────┤
│ • 自动生成交付报告(含完整决策追溯):              │
│                                                 │
│   📊 交付报告                                    │
│   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │
│   ┌─ 需求分析                                    │
│   │  项目类型: 全栈Web · 技术栈: React+FastAPI   │
│   │  已有模块: points/orders/users                  │
│   │                                             │
│   ├─ 推荐方案                                    │
│   │  方案: 扩展现有 API(方案A)                   │
│   │  理由: 复用 points 表,避免新模块,影响最小    │
│   │                                             │
│   ├─ 替代方案(未采纳)                            │
│   │  方案B: 新建独立模块                          │
│   │  未采纳: 与现有架构不一致,引入额外依赖       │
│   │                                             │
│   ├─ 执行摘要                                    │
│   │  子任务: 3/3 完成 · 门禁: 全绿              │
│   │  降级: 无 · 回归: 通过                       │
│   │                                             │
│   └─ 质量分层                                    │
│      loop门禁全绿: 2个任务(高)                   │
│      ZCode直通: 0个任务                         │
│      DeepSeek降级: 0个任务                      │
│                                                 │
│ • 如有 degraded=true 任务 → 高亮提示人工复核     │
│ • 📝 自动合并到主分支(门禁+回归全绿为前提)       │
│   (留 --interactive 标志供用户可选手动模式)      │
│ • 清理 .orchestrate-state.json                  │
└─────────────────────────────────────────────────┘
    │
    ▼
✅ 完成
```

---

## 自动决策原则

| 场景 | v2.0 行为 |
|------|----------|
| L0评估 | 纯规则自动评估→记录理由 |
| **需求分析** | **硬规则: 6维度强制分析(项目身份/技术栈/模块盘点/功能匹配/架构约束/风险方案)→产出报告** |
| **方案推荐** | **基于6维度分析结论 → 给出最优方案 + 替代方案 → 记录采纳和未采纳理由** |
| 拆分确认 | 自动拆分+拓扑排序→直接写入状态 |
| 子任务执行 | ZCode加载 loop --auto 技能,门禁矩阵+自愈闭环全自动 |
| 降级兜底 | loop exhausted → ZCode直连 → DeepSeek,逐级降级不打断 |
| 全局回归 | 自动跑→通过→交付→失败→自动回退重做 |
| 合并决策 | 门禁全绿+回归通过→自动合并(记录理由) |

**唯一仍需人工的地方**(工具限制,非逻辑闸门):
- Cursor 前端任务(v1无CLI)——提示后等commit,这是物理限制
- Trae 审查任务(v1无CLI)——同上

---

## 降级兜底(自动无缝)

loop --auto 内部的 exhausted/阻塞 → 返回给 go → go 降级执行:

```
首选: ZCode + loop --auto(门禁全绿,质量最高)
  ↓ exhausted?
备选: ZCode CLI 直连(无门禁保护,但快速)
  ↓ 429/quota?
兜底: DeepSeek API(标记 degraded,低质量但可交付)
```

自动触发:捕获 429/quota_exceeded → 立即切 DeepSeek,执行不打断。
详见 references/degradation.md。

---

## 三档执行级别

| 维度 | 🟢 L1 直通 | 🟡 L2 标准 | 🔴 L3 完整 |
|------|:---:|:---:|:---:|
| 拆分 | ❌ | ✅ 串行 | ✅ +并行 |
| 需求分析 | 一句话 | 技术选型 | 完整design |
| loop门禁 | 基础验证 | 门禁矩阵 | 矩阵+自愈 |
| 降级 | ❌ | ✅ | ✅ |
| GLM-5.2推理 | off | high | max |
| 合并 | 自动合并 | 自动合并 | 自动合并(加审计摘要) |

---

## 关键约束

1. **⚠️ 硬规则: 必须先深度分析项目实际环境(6维度),才能推荐方案并执行。** 不读项目文档、不扫描现有模块、不检查架构约束 = 禁止进入Step②。
2. **全自动执行**。所有决策遵循推荐→执行→记录,不等人确认。
3. **审计可追溯**。所有自动决策 + 需求分析报告写入 decision_log。
4. **结合项目上下文**。需求分析+方案推荐+验收条件都基于项目实际代码(不凭空猜测)。
5. **L0 纯规则零 token**。不为"省token"而花token。
6. **断点恢复优先**。启动第一件事是检测断点。
7. **降级执行不打断**。配额耗尽自动切 DeepSeek。
8. **原子性保障**。in_progress 任务记 git HEAD,中断恢复 reset 到 HEAD。
9. **编排层不碰执行层**。go传最小信号,ZCode+loop --auto 自行管理Token优化和代码质量。

---

## 参考文档

| 文档 | 内容 |
|------|------|
| `references/complexity-rules.md` | L0 复杂度评估规则 |
| `references/state-protocol.md` | 双轨制状态文件协议 |
| `references/breakpoint-recovery.md` | 断点恢复三重保障 |
| `references/handoff-protocol.md` | 上下文交接协议 |
| `references/degradation.md` | DeepSeek 降级兜底 |
| `references/cursor-collaboration.md` | Cursor 半自动协作 |
| `routing-rules.yaml` | 工具路由规则配置 |
