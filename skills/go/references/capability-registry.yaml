system_review:
  capability: system_consistency_review
  executor_kind: direct_skill
  skill: system-review
  side_effects: none
  compatible_families: [review]

code_review:
  capability: code_level_review
  executor_kind: direct_skill
  skill: code-reviewer
  side_effects: none
  compatible_families: [review]

code_quality_simplify:
  capability: code_craftsmanship_audit
  executor_kind: direct_skill
  skill: clean-code
  side_effects: none
  compatible_families: [review]

debug_root_cause:
  capability: root_cause_analysis
  executor_kind: direct_skill
  skill: systematic-debugging
  side_effects: none
  compatible_families: [debug_fix]

fix_issue:
  capability: targeted_fix
  executor_kind: loop
  skill: null
  side_effects: write
  compatible_families: [debug_fix, design_build]

verify_result:
  capability: completion_verification
  executor_kind: direct_skill
  skill: verification-before-completion
  side_effects: none
  compatible_families: [debug_fix]

research_evidence:
  capability: evidence_gathering
  executor_kind: direct_skill
  skill: evidence-first
  side_effects: none
  compatible_families: [research_compare]

compare_options:
  capability: option_comparison
  executor_kind: direct_skill
  skill: brainstorming
  side_effects: none
  compatible_families: [research_compare]

design_solution:
  capability: requirements_design
  executor_kind: direct_skill
  skill: brainstorming
  side_effects: none
  compatible_families: [design_build]

plan_execution:
  capability: implementation_planning
  executor_kind: direct_skill
  skill: spec-driven-development
  side_effects: none
  compatible_families: [review, research_compare, design_build, web_qa, parallel_investigation]

execute_plan:
  capability: plan_execution
  executor_kind: go
  skill: null
  side_effects: write
  compatible_families: [design_build]

browser_explore:
  capability: browser_bootstrap
  executor_kind: direct_skill
  skill: agent-browser
  side_effects: none
  compatible_families: [web_qa]

browser_regression_test:
  capability: regression_testing
  executor_kind: loop              # 写 e2e/ 脚手架 7+ 文件 + CI workflow
  skill: web-regression-e2e
  side_effects: write
  compatible_families: [web_qa]

browser_visual_diff:
  capability: visual_regression
  executor_kind: loop              # 写 baseline 截图 + diff
  skill: web-visual-diff
  side_effects: write
  compatible_families: [web_qa]

browser_accessibility_audit:
  capability: accessibility_audit
  executor_kind: loop              # 写 a11y 报告（JSON+HTML）
  skill: web-audit-a11y
  side_effects: write
  compatible_families: [web_qa]

browser_perf_audit:
  capability: perf_audit
  executor_kind: loop              # 写 perf 报告（JSON+HTML+summary）
  skill: web-perf-budget
  side_effects: write
  compatible_families: [web_qa]

synthesize_findings:
  capability: findings_synthesis
  executor_kind: direct_skill
  skill: brainstorming
  side_effects: none
  compatible_families: [review, research_compare, web_qa]

parallel_probe:
  capability: parallel_investigation_probe
  executor_kind: direct_skill
  skill: dispatching-parallel-agents
  side_effects: none
  compatible_families: [parallel_investigation]
