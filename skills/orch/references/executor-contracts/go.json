{
  "executor": "go",
  "required_fields": [
    "executor",
    "family",
    "action",
    "goal",
    "feature_summary",
    "plan_path",
    "scope",
    "acceptance_criteria",
    "handoff_inputs",
    "expected_outputs"
  ],
  "expected_outputs": [
    "subtask_results",
    "integration_report",
    "gate_result",
    "delivery_summary"
  ],
  "notes": [
    "Use for side_effects=write nodes that span multiple files, modules, or concurrent subtasks.",
    "go receives an engineering task package, not an already exploded per-file loop task list."
  ]
}
