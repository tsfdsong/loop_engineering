# LoopEngine — 循环工程全家桶

> **设计文档已外部化（2026-07-02）**：spec / plan / ADR 存于独立仓库 [`loop_engineering_specs`](https://github.com/tsfdsong/loop_engineering_specs)，主仓仅保留代码 + 测试 + 安装脚本。详见 [`docs/spec-repo-link.md`](docs/spec-repo-link.md)。
>
> **v1.0.2+ · 7 条全局红线（5 + 2）· 单点真源 · install.sh install_managed_rules() 自动同步**
> **本项目红线版本演进**：v1.0.x（5 条）→ v1.0.2+（7 条，新增进度汇报 + Subagent 边界）
> **用户交互红线 2026-07-01 强化**：从 4 条升级为 5 条（新增"决策点必须用 AskUserQuestion 触发"+ §6 自查清单）
> **摘要输出红线 2026-07-01 强化**：新增"决策点硬约束"（决策点文字必配 AskUserQuestion 工具调用）

---

## 🔴 1. MCP 红线规则（输入端 · 本项目最高优先级 · 不可违反 · v6.4 重构 · 精简版）

> **任何需要理解代码结构的操作，必须先用 MCP 工具，禁止直接 Read 全文件。**
> 此规则适用于本项目（loop_engineering）的所有开发、调研、分析工作。

### 1.1 适用范围

- 修改代码、调研代码、解释代码、分析架构
- 查找函数/类/变量定义、查找引用位置
- 了解项目结构、浏览目录、理解文件内容
- **只要目的是"理解代码"，就必须 MCP 优先**

### 1.2 探查策略（6 场景分层 · v6.11 重构 · 2026-07-01）

**核心原则**：按工作场景分层定义 MCP 使用规则，消除 v1.0.2 旧版"绝对 MCP 优先"的过度泛化。

#### 场景矩阵（S1-S6）

| 场景 | 工作场景 | 推荐工具 | 必须性 |
|---|---|---|---|
| **S1** | 接入新代码库（首次理解项目结构） | `get_repo_map` → `get_file_outline` | **必须** |
| **S2** | 探索大文件结构（>500 行，功能未知） | `get_file_outline` + `search_symbols` | 建议 |
| **S3** | 修改已知位置（行号已知或符号已知） | `Read` (offset/limit) **或** `get_symbol_source` | 直接合规 |
| **S4** | 跨文件搜索引用（找调用方/被调用方） | `check_references` / `find_importers` | 建议 |
| **S5** | 跨文件关键字搜索（grep 类） | `search_text` / `search_ast` | 替代 Bash grep |
| **S6** | 失败/不可用时 fallback | `repomix.pack_codebase` | 必须 fallback |

**必须性定义**：
- **必须**：违反触发自检警告，AI 应主动解释为何未用
- **建议**：默认使用，AI 可基于效率判断 fallback
- **直接合规**：任何方式都 OK，不需解释

#### 工作流（修订版）

```
任务开始 → 判断场景
├─ S1 接入新代码库 → 必须 L0 (get_repo_map) → L1 (get_file_outline)
├─ S2 大文件结构   → 建议 L1 (get_file_outline) + L2 (search_symbols)
├─ S3 修改已知位置 → 直接 Read (offset/limit) 或 L2 (get_symbol_source)
├─ S4 跨文件搜索   → 建议 L2 (check_references / find_importers)
├─ S5 跨文件关键字 → 建议 L2 (search_text / search_ast)
└─ S6 MCP 失败     → 必须 fallback (repomix → Read)
```

> **变更说明（v6.11 · 2026-07-01）**：原 L0-L3 漏斗被作为绝对命令，导致 70% "修改已知位置"场景被误判违规（meta-analysis 数据）。本次重设计将漏斗嵌入 S1-S6 场景矩阵，**保留** S1/S2 探索场景的 MCP 优先原则，**承认** S3 直接 Read 的精准优势。依据：`docs/superpowers/specs/2026-07-01-mcp-redesign.md` §2.1。

### 1.3 MCP 三件套职责分工

| 工具 | 最佳场景 | 兜底场景 | 限制 |
|------|---------|---------|------|
| **jCodeMunch-MCP** | Python 代码（AST 解析） | 任意结构化文件 | 需先 index_folder；worktree 默认未索引 |
| **Repomix** | 任意代码库（含 Markdown） | jcodemunch 索引失败时 | 一次性输出，无增量 |
| **Headroom-ai** | 大段 Markdown / 大文件压缩 | 长会话持续压缩 | hash 检索，不变更内容 |

### 1.4 唯一例外（5 条）

| # | 例外场景 | 处理 |
|---|---------|------|
| 1 | MCP 工具全部不可用（报错/超时/未索引） | 记录原因后用 Read，但**仍要尝试 repomix 兜底** |
| 2 | 文件 < 50 行（小配置文件等） | 可直接 Read |
| 3 | 已通过 MCP 定位，需要精确读取 1-3 行 | Read with offset/limit |
| 4 | **执行类操作**（git/cp/rsync/worktree） | 用 Bash，**不算违规** |
| 5 | **JSON/YAML/TOML 小配置**（< 30 行） | Read 全文，**不算违规** |

### 1.5 违规判定（4 档 · v6.11 场景感知 · 2026-07-01）

**核心变更**：从"通用阈值"（连续 3 Read、单次 > 100 行）改为"按场景判定"。原 §1.5 的"连续 3 次 Read 即红线"误判 S3 场景，予以删除。

| 等级 | 违规行为（按场景） | 自愈方法 |
|------|-------------------|---------|
| 🔴 **红线** | **S1 接入新代码库**未用 `get_repo_map` 直接 Read | 立即重写会话，先用 MCP 探查 |
| 🟠 **严重** | **S5 跨文件关键字搜索**未用 `search_text` 直接 Bash grep | 改用 `search_text` / `search_ast`（或 Read with offset/limit） |
| 🟠 **严重** | worktree 中未先尝试 `index_folder` 直接 Read | 优先 `index_folder(identity_mode="git")`；失败 → repomix 兜底 |
| 🟡 **中等** | MCP 不可用时未尝试 `repomix.pack_codebase` 兜底 | 自动调 repomix 兜底 |
| 🟡 **中等** | 长会话（> 30 轮）未用 `headroom_compress` | 定期压缩大段内容 |
| ✅ **合规** | **S3 修改已知位置**直接 Read（offset/limit） | 不算违规 |
| ✅ **合规** | **S5 单文件 grep** 直接用 Bash | 不算违规（仅限单文件） |
| ✅ **合规** | 文件 < 50 行（沿用 §1.4 例外 2） | 不算违规 |

> **变更说明**：原"连续 3 次 Read 即红线"过度泛化，70% 的"修改已知位置"场景被误判。`✅ 合规` 档明示 S3 / S5 单文件 / 小文件场景直接 Read 不算违规。依据：spec §3.2。

### 1.6 自查清单（5 项 · 场景感知 · v6.11 · 2026-07-01）

> **变更说明**：原 §1.6 的 5 项以"MCP vs Read 计数"为二元指标，误判 S3 场景。改为按 S1-S6 场景自检。

- [ ] **S1 接入新代码库**是否先 `get_repo_map` → `get_file_outline`？（漏斗价值最高）
- [ ] **S3 修改已知位置**是否用 `Read` (offset/limit) 而非 Read 全文？（直接合规）
- [ ] **S4 跨文件搜索**是否优先 `check_references` / `find_importers`？（MCP 强项）
- [ ] **worktree 中**是否先尝试 `index_folder(identity_mode="git")`，失败再 fallback `repomix.pack_codebase`？（自动降级）
- [ ] **长会话**（> 30 轮）是否定期 `headroom_compress` 大段内容？（防止上下文爆炸）

### 1.7 worktree 特殊流程（v6.11 自动化 fallback · 2026-07-01）

> **变更说明**：原 v6.4 流程要求"创建 worktree 后**立即** index_folder"（硬性命令），但 worktree 经常被 jcodemunch `trusted_folders` 拒绝，导致首次失败 → 心理放弃 → 退到 Bash/Read。新版改为"**优先尝试 + 自动 fallback**"。

**优先级自动反转**：worktree 中默认仍应尝试 MCP（因失败成本低），但失败时**自动**降级 repomix，**不再**算违规。

```bash
# 1. 进入 worktree → 优先尝试 MCP 索引
mcp__jcodemunch__index_folder(
  path=".worktrees/<name>",
  identity_mode="git",        # 关键：让 jcodemunch 识别为同一 git repo
  follow_symlinks=true        # 跟随 git worktree 符号链接
)

# 2. 失败 → 自动 fallback 到 repomix（不报错、不算违规）
mcp__repomix__pack_codebase(directory=".worktrees/<name>")

# 3. 仍失败 → 才 fallback 到 Read with offset/limit（最后手段）

# 4. 验证可解析（仅 S1 接入新代码库场景必须）
mcp__jcodemunch__resolve_repo(path=".worktrees/<name>")
```

**关键差异（vs v6.4）**：
- ✅ "立即索引" → "优先尝试"（失败不阻断）
- ✅ `resolve_repo` 步骤 2 → 步骤 4（仅 S1 场景必须）
- ✅ "失败兜底" → "自动 fallback"（语义降级）
- 🆕 失败时由 repomix 接续，不需 AI 手动判断

### 1.8 Bash 探查职责清单

| ✅ 允许（执行类） | ❌ 禁止（探查类，应改 MCP/Read） |
|------------------|---------------------------------|
| `git log/show/diff` | `cat < file >` |
| `ls/find`（仅看文件名） | `head -N < file >`（探查内容） |
| `cp/rsync/rm/worktree add` | `grep -rn "pattern" *.py`（探查代码） |
| `git rm/mv/add/commit` | `wc -l < file >`（探查行数） |
| `mkdir/touch/chmod` | `awk/sed`（修改文件） |

### 1.9 工具速查 + 性能基准 + PATH 修复（已迁移，单一真源）

> 以下 3 节在 v1.0.2 已迁移：
> - **工具速查**（L0-L2 → jcodemunch / repomix / headroom 三件套选型）→ 已并入 §1.2 "4 层探查"概念图
> - **性能基准**（6 场景实测数据）→ `docs/mcp-benchmark.md`
> - **PATH 修复指引**（MCP 命令找不到）→ `docs/mcp-setup-guide.md` 第四章

---

## 🔴 2. 事实优先硬规则（分析端 · 本项目最高优先级 · 不可违反 · 2026-06-29 加入）

> **针对 2026-06-29 v5.4 兼容性胡乱分析事故**（AI 基于错误假设套用通用话术被用户当场识破），
> 本项目强制执行以下 5 条事实优先规范：

1. **项目分析前必查 5 项事实**（参见 `skills/evidence-first/SKILL.md`）：
   - `git log --oneline -10`（最近 10 次提交）
   - `README.md` / `AGENTS.md`（项目自述）
   - `pyproject.toml` / `package.json`（版本声明）
   - `docs/` 下最近的设计文档 / CHANGELOG
   - `git log --since="3 months ago" --oneline | wc -l`（3 个月活跃度）

2. **每句话必须标注 [F]/[H]/[P]**：
   - `[F]` 事实（已通过 git/file/docs 验证，可追溯）
   - `[H]` 假设（基于经验但未验证，需明确标注）
   - `[P]` 原则（通用工程参考，优先级最低）

3. **不确定 = 说"我不清楚"**：禁止凑答案 / 编造 / 套模板

4. **长篇论述前自检 4 问**：
   - 我有 [F] 事实依据吗？
   - [H] 假设明确标注了吗？
   - 错了损失大吗？（高损失必须 [F] 主导）
   - 能说"我不清楚"吗？

5. **判断必须可追溯到事实**：每条结论附"事实依据 + 来源 + 验证方式"

**未完成事实清单 = 禁止进入分析论述**。

**违规判定**：长篇论述（> 5 段）无 [F] 标注 = 视为红线违规，自动加载 `evidence-first` 技能重写。

**事故教训库**：`docs/lessons-learned.md`（含本事故的完整记录 + 修复措施）

---

## 🔴 3. 用户交互红线（沟通端 · 全局适用 · 单点真源 · v6.10 升级：5 条 + §3.6 自查清单）

> **任何涉及用户决策、方案选择、确认的交互，必须遵循以下 5 项硬要求。**

1. **必须使用 `AskUserQuestion` 工具** — 列出 2-4 个选项（含推荐），让用户通过选择框选择，**不允许要求用户输入自由文本**
2. **推荐项必须标注 `(推荐)` 并说明理由** — 第一个选项为推荐项，描述中说明为什么推荐
3. **不推荐项必须说明理由** — 每个非推荐选项的描述中说明不推荐或需谨慎的理由
4. **禁止开放式追问** — 不允许"你觉得呢？""还需要什么？""你还想做吗？"等需要用户组织语言回答的提问
5. **🆕 决策点必须用 AskUserQuestion 触发**（2026-07-01 加入）— 任何"## 🎯 决策点 / 决策 X / 是否要..."的输出，**必须**配对 `AskUserQuestion` 工具调用，**禁止**用 markdown 文字列决策点。**这是硬约束**：决策点文字 + 不调 AskUserQuestion = 红线违规。

**违反以上任一条 = 视为阻塞 Bug，必须自愈修复。**

### 3.5 触发场景清单（5 类 · 必须用 AskUserQuestion）

| # | 场景 | 示例 | 错误做法 |
|---|------|------|---------|
| 1 | 方案选型 | "A 方案 vs B 方案" | 文字列"建议 A" |
| 2 | 确认/批准 | "是否 merge 这个分支？" | 文字列"建议 merge" |
| 3 | 范围裁剪 | "修复 a/b/c/d 哪几项？" | 文字列"建议 a+c" |
| 4 | 优先级排序 | "先做 X 还是 Y？" | 文字列"建议先 X" |
| 5 | 二元开关 | "开启/关闭 X 功能？" | 文字列"建议开" |

**豁免场景**（无需 AskUserQuestion）：
- 闲聊 / 简单问答
- 用户已明确"不要求决策，直接做"
- 用户已经在前文给出明确指令（如"做 X" → 无需再问）

> **适用范围**：本项目（loop_engineering）所有技能、所有场景。当任一技能需要请你决策、确认、选择方案时——一律遵循本红线。
> **来源**：单一真源 = 本章节。各技能 SKILL.md **不得重复**承载此规则（避免漂移）。

### 3.6 自查清单（5 项 · 会话结束前必查）

- [ ] **本轮决策点是否全部用 `AskUserQuestion` 触发？**（v6.10 新增 · 关键项）
- [ ] **每个 AskUserQuestion 调用是否含 2-4 个选项 + 1 个标注 `(推荐)`？**
- [ ] **不推荐选项的描述是否说明了不推荐理由？**
- [ ] **是否避免了"你觉得呢？""还需要什么？"等开放式追问？**
- [ ] **是否避免了"建议 X"但又不调 AskUserQuestion 的混合形式？**（最常见的违规模式）

> **违规判定**：决策点用文字列出 + 不调 AskUserQuestion = 🔴 红线违规，必须自愈。

### 3.7 🆕 v6.11 强化（2026-07-02 加入）· 建议列选项也走 AskUserQuestion

> **背景**：2026-07-02 system-review v2 C-lite 报告末尾出现"可选 A/B/C"未配 AskUserQuestion 的违规，揭示出红线 3 v6.10 仍有漏洞——AI 在写"建议 / 如需 / 后续步骤"型段落时会下意识列选项但跳过工具调用。

**硬规则扩展**：
- 红线 3.5 第 1 类（方案选型）适用范围扩展 → **任何含选项列表的输出**（决策点 / 建议 / 如需 / 后续步骤 / 候选方案）**必须走 AskUserQuestion**，禁止用 markdown 列表形式罗列选项让用户"看着挑"。
- §3.6 自查清单新增第 6 项（见 §3.7.1）。
- 红线 3 整体升级为：**v6.11 = 5 条 + §3.6 自查清单 6 项 + §3.7 建议列选项强化**。

#### 3.7.1 §3.6 自查清单第 6 项（v6.11 新增）

- [ ] **本轮"建议 / 如需 / 后续步骤 / 候选方案"段落是否含 ≥ 2 个候选？如有，是否全部用 AskUserQuestion 触发？**（v6.11 新增 · 防止"建议 + 列表"逃逸）

#### 3.7.2 违规判定

- 建议段落含 ≥ 2 个候选但未调 AskUserQuestion = 🔴 红线违规（与"决策点"违规同级）
- 单一推荐（如 `**建议 X，因为 Y**`）= 合规（无选项列表，不触发 AskUserQuestion）
- 纯事实陈述 / 引用 / 清单（无选项性内容）= 合规

#### 3.7.3 6 条避免方法（v6.11 升级）

1. 写 "## 🎯 决策点" 之前先自问 → **立即调** AskUserQuestion（不要先写 markdown 再调工具）
2. 红线 3.6 五项 checklist 每次输出结束前必走
3. "如需..."型句子默认触发 AskUserQuestion
4. 区分"建议"与"决策"的写法：
   - **建议 + 单一推荐**（合规）：`**建议 X，因为 Y**`
   - **建议 + 多候选**（违规 → 改用 AskUserQuestion）：禁止 markdown 列表
   - **决策 + 多选项**（必须 AskUserQuestion）
5. 养成"先调 AskUserQuestion 再写 markdown 选项"的肌肉记忆
6. **🆕 自检触发条件**：任何输出末尾含"如 / 或 / 可以 / 推荐 / 建议 / 候选" + 选项列表 → 必走 AskUserQuestion

> **事故记录**：2026-07-02 system-review v2 C-lite 报告末尾违规案例已记录（本会话已自愈 + AGENTS.md 已升级）。详见本会话对话历史。

---

## 🔴 4. 摘要输出红线（产出端 · 全局适用 · 单点真源 · 2026-06-30 加入 · v6.10 升级决策点硬约束）

> **针对 2026-06-30 用户反馈**（"AI 总是输出完整长文，无法快速定位核心要点做决策"），
> 本项目强制要求 AI 在产出"决策依据类"内容时，**末尾必须附加结构化摘要**。

### 4.1 触发场景（必须输出摘要）

- 建议 / 推荐方案（"推荐用 A 方案…"）
- 报告（调研报告 / 审查报告 / 复盘 / changelog）
- 思考分析（架构选型 / 问题诊断 / 方案对比）
- 多步执行结果汇报（含决策、风险、后续行动）

### 4.2 豁免场景（无需摘要）

- 闲聊 / 简单问答（"你好"、"版本号多少"）
- 单行错误信息 / 报错堆栈
- 执行类操作的纯状态汇报（`git commit 成功`、`文件已创建`）
- 用户明确要求"不要摘要 / 给我完整详情"

### 4.3 摘要结构（固定模板）

```markdown
## 📌 核心摘要
- 要点 1：核心结论 / 关键数据
- 要点 2：建议方案 / 推荐项
- 要点 3：风险 / 注意事项 / 已知边界
- 要点 4..10：（可选，可复杂场景）

## 🎯 决策点（仅当存在需你拍板的事项时）
- 决策 1：[选项 A] / [理由]
- 决策 2：[选项 B] / [理由]
```

> ⚠️ **🆕 决策点硬约束（v6.10 加入）**：
> - "## 🎯 决策点" 不是单纯的 markdown 输出 — 它是 **`AskUserQuestion` 工具调用的文字版预览**
> - 每个决策点 = 1 个 AskUserQuestion 调用 + 2-4 个选项 + 1 个 `(推荐)` 标注
> - 决策点文字 + 不调 AskUserQuestion = 🔴 红线违规（用户交互红线第 5 条）
> - 例外：如果本轮**没有**决策点，**不输出** "## 🎯 决策点" 章节（不是必须项）

- bullet 数量：**3 ≤ N ≤ 10**（超出视为二阶长文，需砍次要项）
- 涉及事实的 bullet 仍要遵守「事实优先硬规则」加 [F]/[H]/[P] 标注
- 摘要位置 = 最后输出（工具调用结果之后、分隔符/签名之前）

### 4.4 违规判定

| 违规行为 | 自愈方法 |
|---------|---------|
| 触发场景却未带 `## 📌 核心摘要` | 红线违规 → 自动重写追加 |
| 摘要 bullet < 3 个 | 视为冗余 → 扩展到 ≥ 3 个 |
| 摘要 bullet > 10 个 | 视为二阶长文 → 砍次要项 |
| 存在需拍板事项却无 `## 🎯 决策点` | 漏标 → 补独立子区（必须配对 AskUserQuestion） |

### 4.5 自查清单（会话结束前必查）

- [ ] 本次输出末尾是否有 `## 📌 核心摘要`？（触发场景触发时）
- [ ] 每个 `## 🎯 决策点` 是否配对了 `AskUserQuestion` 工具调用？（v6.10 硬约束）

> **来源**：单一真源 = 本章节。各技能 SKILL.md **不得重复**承载此规则（避免漂移）。

---

## 🔴 5. 完成前验证红线（诚信端 · 全局适用 · 单点真源 · 2026-06-30 加入 · v6.9 升级）

> **针对大模型"幻觉式完成"问题**（AI 在未真正执行任务时宣称已完成，违反诚信），
> 本项目强制执行"证据先行"铁律：**无验证证据 = 无完成声明**。

### 5.1 铁律（Iron Law）

```
NO COMPLETION CLAIMS WITHOUT FRESH VERIFICATION EVIDENCE
```

"完成"的三要素（**三者缺一 = 不得宣称完成**）：

1. **任务已实际执行** —— 不是"准备执行"、不是"应该执行了"
2. **结果已实际输出** —— 不是"理论上能输出"、不是"应该输出了"
3. **验证命令已运行** —— 在**本轮消息**中跑过命令、读过输出

### 5.2 触发场景（10 类常见执行任务 · 必须验证后才能宣称完成）

| # | 任务类型 | 验证证据（必须） |
|---|---------|---------------|
| 1 | 项目调研 | 调研报告已写入文件 / 已在输出中呈现（含具体发现） |
| 2 | 网络搜索 | `WebFetch` / `WebSearch` 已返回实际结果（不是"调用了"工具） |
| 3 | 代码探查 | MCP 工具已返回实际代码内容 / 符号源码（不是"探查了"） |
| 4 | 代码分析 | 分析结论已附带具体行号 / 文件路径 / 符号引用 |
| 5 | 结构设计 | 设计文档已写入文件 / 文档大纲已呈现 |
| 6 | 功能实现 | 代码已实际写入文件（`git diff` 可查） |
| 7 | 代码编写 | 文件已创建 / 已修改（`ls` 或 `git status` 可证） |
| 8 | 测试运行 | 测试命令已执行 + 输出已读取 + 失败计数已确认 |
| 9 | Bug 修复 | 原始症状复现测试 + 修复后测试已通过（**红绿循环**验证） |
| 10 | 任何"已完成"声明 | 验证命令已在**本轮消息**运行 + 输出已读取 |

### 5.3 豁免场景（无需验证）

- 闲聊 / 简单问答（"你好"、"版本号多少"）
- 用户明确表示"不要求验证 / 跳过验证"
- 纯建议 / 方案推荐（不涉及"已完成"声明）
- 与摘要红线相同的豁免：单行错误信息 / 执行类纯状态汇报 / 用户明确要求"不要摘要"

### 5.4 违规判定（6 类典型违规）

| 违规行为 | 自愈方法 |
|---------|---------|
| 宣称"已完成 X"但 X 的验证命令未在本轮运行 | 红线违规 → 立即重跑验证 + 客观陈述结果 |
| 工具调用失败 / 超时 / 报错，仍宣称"已完成" | 红线违规 → 客观陈述失败状态，**不得**宣称完成 |
| 输出被截断 / 部分完成，仍宣称"已全部完成" | 红线违规 → 标注未完成部分 |
| 突发不可逆状态（会话中断 / 上下文丢失 / 工具崩溃），仍宣称"已完成" | 红线违规 → **只**客观陈述不能完成的现状，**不得**宣称已完成 |
| 使用"应该完成"、"差不多"、"已经做了"等模糊措辞 | 视为违规 → 必须给出具体证据（行号 / 退出码 / 失败计数） |
| 转述"agent / 工具报告成功"而未独立验证 | 视为违规 → 必须独立验证（VCS diff / 输出重读） |

### 5.5 自查清单（会话结束前必查）

- [ ] 本轮消息中所有"已完成"声明是否都有对应的验证命令运行？
- [ ] 验证命令的输出是否已**实际读取**（不是"看起来 OK"）？
- [ ] 工具调用失败时是否已客观陈述失败状态（未宣称完成）？
- [ ] 输出截断时是否已标注"未完成"部分？

> **来源**：单一真源 = 本章节。**详细方法论**（铁律 + Gate Function + 常见失败表 + 借口防范表）见 `skills/verification-before-completion/SKILL.md`；本章是**红线版**（必须遵守的硬约束）。两者**并存互引** —— 红线指向技能的方法论，技能指向红线的硬约束。

---

## 🔴 6. 进度汇报红线（运行端 · 单点真源 · 2026-07-01 加入）🆕

> **针对长任务"假死感"痛点**（用户不知道 AI 在干什么、是否还活着、何时完成），
> 本项目强制执行"运行端节奏控制"铁律：**预估 ≥ 10 步 或 > 2 分钟的任务，必须分段汇报进度**。

### 6.1 铁律

预估执行步骤 ≥ 10、调用 subagent、或总时长预期 > 2 分钟的任务，必须在**每 5-10 步或每主要阶段完成时**输出结构化进度，禁止 > 30 分钟静默。

### 6.2 触发场景（必须汇报）

- 多步骤执行（≥ 10 步工具调用）
- 多文件改动（≥ 5 个文件）
- 跨 skill 编排（orch 调度 ≥ 2 个 skill）
- 调用 subagent（dispatching-parallel-agents / subagent-driven-development）

### 6.3 豁免场景（无需汇报）

- 单步操作（1-2 工具调用）
- 短任务（< 5 步 / < 1 分钟）
- 用户明确表示"不需要进度，直接做完"

### 6.4 进度结构（固定模板）

```markdown
## 📊 进度汇报 (N/M)
- **已完成**：[步骤列表，简短]
- **当前阶段**：[具体阶段名]
- **下一步**：[即将做什么]
- **阻塞**：[如有，未阻塞则填"无"]
- **预计剩余**：[X 步 / Y 分钟]
```

### 6.5 与"摘要输出红线"的协调

- **进度汇报**：**中途**节奏控制（每 5-10 步）
- **摘要输出**：**任务结束**结构化摘要
- 两者不冲突：进度汇报简短，结束时归入完整摘要

### 6.6 违规判定

- 满足触发条件但 > 30 分钟无任何状态输出 → 红线违规
- 仅转述"还在做"、"差不多了"等模糊措辞 → 红线违规 → 必须用固定模板结构
- 与完成前验证红线冲突时优先级：**完成前验证（诚信端） > 进度汇报（运行端）**

---

## 🔴 7. Subagent 边界红线（执行端 · 单点真源 · 2026-07-01 加入）🆕

> **针对 subagent 派遣碎片化、未升格为顶层硬规则问题**（项目内 subagent 协议散落在 5 处 SKILL.md，未统一为 AGENTS.md 顶层规则），
> 本项目强制执行"执行端越界防控"：**主 agent 不得单点依赖 subagent 报告，须独立验证 + 防越界硬规则**。

### 7.1 铁律

调用 Agent 工具 / dispatching-parallel-agents / subagent-driven-development 时，主 agent 必须：

1. **为 subagent 构造完整上下文**（never inherit session context）
2. **派后独立验证 subagent 返回**（不得仅转述报告）
3. **严格遵循 4 不派发条件 + 12 条 Red Flags**

### 7.2 触发场景

- 调用 Agent 工具（所有 .pi / .opencode / Codex / Gemini 的 Task/Subagent）
- 使用 `/orch` 编排且任务链含 subagent
- 使用 dispatching-parallel-agents / subagent-driven-development

### 7.3 subagent 必接 5 类输入

1. **specific scope**（具体范围）
2. **clear goal**（清晰目标）
3. **constraints**（约束条件：时间 / token / 工具白名单）
4. **output format**（返回格式：JSON / Markdown / 状态枚举）
5. **context snippets**（必要代码片段，禁止 subagent 重复探索）

> 详细规范见：
> - `skills/dispatching-parallel-agents/SKILL.md` L62-74（派遣 API + Good agent prompts）
> - `skills/subagent-driven-development/SKILL.md` L14-21（核心原则）+ L242-271（Red Flags Never 清单）
> - `skills/go/references/handoff-protocol.md` L21-49（结构化 JSON handoff）

### 7.4 主 agent 独立验证义务

- **不得**仅转述"subagent 报告说完成"
- 必须独立读代码 / 看 diff / 跑命令验证（与"完成前验证红线"互补）
- 强制 3 阶段审查（仅 subagent-dd 模式）：implementer → spec reviewer → code quality reviewer
- 4 状态协议：DONE / DONE_WITH_CONCERNS / BLOCKED / NEEDS_CONTEXT

### 7.5 4 不派发（硬条件）

- 问题之间共享状态（需协调而非并行）
- 需全 session 上下文（subagent 不继承）
- 探索性调试（结果不确定 → 单步更可控）
- 顺序依赖（前一步输出决定后一步输入）

### 7.6 违规判定

- subagent 返回未经独立验证即宣称完成 → 红线违规
- 派发时不传 5 类输入 → 红线违规
- 违反 4 不派发条件强行并行 → 红线违规
- 与"完成前验证红线"冲突时优先级：**完成前验证（诚信端） > Subagent 边界（执行端）**

### 7.7 已知边界（v1.0.2 暂未实现）

| # | 缺失能力 | 计划 |
|---|---------|------|
| 1 | 工具白名单（subagent 工具权限收敛） | 后续 v6.x 桥接层完成 |
| 2 | token 预算（限制 subagent 读大文件全量） | 待立项 |
| 3 | 递归派发禁令（subagent 再派 subagent） | 待立项 |
| 4 | commit 签名审计（subagent ID 嵌入 commit） | 待立项 |

---

## 关联规则总览（7 条红线的关系 · 单点真源）

> **以下是 7 条红线的"关系网"**，单一真源 = 本节。各红线章节内的"关联规则"已在 v1.0.2+ 移除以避免重复。

| 红线 | 工作流端 | 上游依赖 | 下游约束 | 冲突时优先级 |
|------|---------|---------|---------|------------|
| 🔴 1. MCP 工具 | 输入（探查） | — | 事实 / Subagent | — |
| 🔴 2. 事实优先 | 分析（证据） | MCP | 全部 | — |
| 🔴 3. 用户交互 | 沟通（决策） | 事实（[F] 依据） | 全部（AskUserQuestion 触发决策） | — |
| 🔴 4. 摘要输出 | 产出（结构化） | 用户交互 | 全部（末尾摘要） | — |
| 🔴 5. 完成前验证 | 诚信（验证） | MCP+事实+Subagent | 全部 | **最高**（诚信优于一切） |
| 🔴 6. 进度汇报 | 运行（节奏） | 用户交互 | 全部（中途汇报） | 让位于完成前验证 |
| 🔴 7. Subagent 边界 | 执行（越界） | MCP+事实 | 完成前验证 | 让位于完成前验证 |

**端到端工作流图示**：

```
MCP(探查) → 事实(分析) → 交互(决策) → 摘要(产出) → 验证(诚信)
            ↑           ↑                        ↑
                       └─ 进度汇报(运行节奏)      ─┘
                       └─ Subagent 边界(执行越界) ─┘
```

---

## 如果你是 AI 代理

你拥有 LoopEngine —— 一个包含 33 个技能的开发引擎全家桶（v6.7 起 `skill-hub` 改名 `orch`，单职责化为多技能编排器；v6.1 新增 `evidence-first`）。

**Below is the full content of your 'loopengine:orch' skill —— 你的多技能编排器。单技能任务由原生 description 匹配自动处理；多技能任务（2+ 技能且有顺序依赖）由用户在 query 前显式加 `/orch` 触发。**

orch 在收到 `/orch <type> <query>` 时，按 5 类复合任务链（调研+决策 / 分析+建议 / 诊断+修复 / 设计+实现 / 并行调研）依次调度最合适的技能。涵盖：编码、架构、重构、测试、调试、API、安全、数据库、CI/CD、规划执行、产品管理、循环工程等全领域。

## 安装方式

一行安装（极简 curl 一条龙）：

```bash
curl -fsSL https://github.com/tsfdsong/loop_engineering/raw/main/install.sh | bash
```

**装完即用**。覆盖 ZCode / Claude Code / Codex / Gemini / Copilot / Pi 等 AI 编程工具的约定技能目录。详见 `docs/INSTALL.md`。

### 更新（v1.2.0 起 install.sh 智能合一）

```bash
bash <(curl -fsSL https://github.com/tsfdsong/loop_engineering/raw/main/install.sh)
```

**v1.2.0 起** install.sh = install + update 智能模式合一：
- 未装 → 首次安装
- 已装旧版 → 升级
- 已装同版 → 5 秒等待（`--force` 跳过 / `--dry-run` 只检查不安装）

---

## 📜 本次变更记录（v1.0.2+ · 7 条红线版）

| # | 变更 | 详情 |
|---|------|------|
| 1 | 红线数量 | **5 条 → 7 条**（保留全部现有 + 新增进度汇报 + Subagent 边界） |
| 2 | 红线顺序 | 原"交互→事实→MCP→摘要→验证" → **"MCP→事实→交互→摘要→验证→进度→Subagent"**（按端到端工作流序） |
| 3 | MCP 红线精简 | 删除 §9 工具速查表 + §10 性能基准表 + §11 PATH 修复 → 改为 1 行引用 + 单一真源迁移 |
| 4 | 关联规则去重 | 5 条红线尾部"关联规则"小段 → 统一到末尾"关联规则总览"单点 |
| 5 | 保留用户 2026-07-01 强化 | 用户交互红线第 5 条决策点硬约束 + §3.6 自查清单 + 摘要输出红线 v6.10 决策点段（原样保留，**未覆盖**） |
| 6 | 新增红线 6 | 🔴 进度汇报红线（运行端） |
| 7 | 新增红线 7 | 🔴 Subagent 边界红线（执行端） |
| 8 | 顶部版本注释 | 新增 v1.0.2+ + 7 条红线说明 + 2026-07-01 强化记录 + 演进路径 |
| 9 | 历史段整合 | 采纳用户最新简化版（v1.1 起），不重复旧 ZCode 文档（已归档 docs/legacy/） |
