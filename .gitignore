
# go 编排层 worktree 目录
.go/worktrees/
__pycache__/
node_modules/

# deep-research 工作产物（调研已完成，不入库；本地保留供跨会话恢复）
.workflow/

# 历史遗留空目录（test-page 内容已迁移；Windows 文件锁无法直接 rmdir）
test-page/

# 设计文档已外部化（2026-07-02）：spec/plan 在独立仓库 loop_engineering_specs
# 本地缓存路径 ~/.loopengine/specs/ 由 install.sh clone 管理，不在主仓
# 保留 7 天过渡期（2026-07-02 → 2026-07-09），届时手动删除
docs/loopengineering-design/
docs/superpowers/

# IDE / 本机 AI 工具项目级配置（非插件打包内容）
.idea/
.vscode/
.claude/
.zcode/
.worktrees/

# 本机 MCP 路径（可选；勿提交含绝对路径的本机配置；写法见 docs/mcp-setup-guide.md）
.mcp.json

# 验证 Gate 状态目录（运行时产物，不入库）
# 包含 verdict.json / evidence-log.jsonl / has_code_changes 等会话级证据
.verify-state/

# 临时调试产物（不入库）
err.txt

# 本地 spike / 实验产物（不入库）
.spike-zcode-goal/
