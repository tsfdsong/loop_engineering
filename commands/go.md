---
description: 跨模块工程化执行（worktree 隔离 + 自动拆分 + 系统审查）
allowed-tools: Bash, Read, Edit, Write, Agent, TodoWrite
---

使用 `go` 技能执行跨模块工程任务。

加载 `skills/go/SKILL.md` 获取工程化执行方法论。

## 触发词

`/go`、编排、worktree 并发、多模块、多技能编排、2+ 技能组合、family 路由

## 不适用

- 单任务闭环（用 `/loop`）
- 纯调研（用 `deep-research`）
- 单文件修改 / 简单重构
